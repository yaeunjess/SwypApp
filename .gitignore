# IDE & 빌드 설정 관련 (Android Studio / IntelliJ)
*.iml                           # IntelliJ 프로젝트 모듈 파일 (개인 경로 포함)
.gradle                         # Gradle 빌드 시스템의 로컬 캐시 데이터
/.idea/caches                   # IDE 내의 파일 인덱싱 및 캐시
/.idea/libraries                # 프로젝트 라이브러리 설정 (자동 생성됨)
/.idea/modules.xml              # 모듈 정보 관리 파일
/.idea/workspace.xml            # 마지막으로 연 파일, 창 크기 등 개인 작업 설정
/.idea/navEditor.xml            # Navigation Editor 설정 데이터
/.idea/assetWizardSettings.xml  # 이미지 에셋 생성 시 저장된 설정값

# 보안 및 비밀 정보 (GitHub에 절대 올리면 안 됨)
/local.properties               # SDK 경로 및 API Key(카카오, 구글 등) 저장 파일
local.properties                # (중복 방지용) 루트 외 폴더의 설정 파일 방지
*.jks                           # 앱 배포용 서명 키 (유출 시 앱 탈취/사칭 위험)
*.keystore                      # 구글 앱 서명용 구형 키 저장소 파일
google-services.json            # Firebase 접속용 개인 키 (유출 시 DB 접근 위험)

# 빌드 결과물 및 임시 파일 (재생성 가능)
/build                          # 컴파일된 결과물 (APK, 클래스 파일 등 - 고용량)
/captures                       # 성능 분석 및 화면 캡처 파일 저장소
.externalNativeBuild            # C/C++(NDK) 빌드 시 발생하는 중간 파일
.cxx                            # C++ 빌드 프로세스용 데이터

# 시스템 부산물 (OS별 불필요한 파일)
.DS_Store                       # macOS에서 자동으로 생성하는 폴더 정보 파일
Thumbs.db                       # Windows에서 이미지 미리보기를 위해 생성하는 파일

# 기타 로그 및 테스트
*.log                           # 앱 실행 중 발생한 오류/로그 기록 파일
/ASMCoreStats                   # 안드로이드 스튜디오 테스트 결과 리포트